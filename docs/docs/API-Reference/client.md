# Client

`UAEFClient` calls a deployed UAEF service over HTTP instead of evaluating in-process. Use
it when the heavy metric backends should live in the service, not in your application.

::: uaef.client
